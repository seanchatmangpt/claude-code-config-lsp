//! ConformanceVector — AUTO-GENERATED by ggen lsp-max pack
//! Source: schema/domain.ttl + queries/conformance.sparql
//! Edit the .ttl, run `ggen sync` — never edit this file directly

use lsp_max::max_protocol::{ConformanceVector, LawAxis};

pub fn conformance_vector() -> ConformanceVector {
    ConformanceVector {
        // Methods with real handler implementations
        admitted: vec![
            LawAxis::Custom("textDocument/didOpen".to_string()),
            LawAxis::Custom("textDocument/didChange".to_string()),
            LawAxis::Custom("textDocument/didClose".to_string()),
            LawAxis::Custom("textDocument/diagnostic".to_string()),
            LawAxis::Custom("textDocument/hover".to_string()),
            LawAxis::Custom("textDocument/completion".to_string()),
        ],
        // Methods explicitly not implemented — clients must not expect responses
        refused: vec![
            LawAxis::Custom("callHierarchy/incomingCalls".to_string()),
            LawAxis::Custom("callHierarchy/outgoingCalls".to_string()),
            LawAxis::Custom("codeLens/resolve".to_string()),
            LawAxis::Custom("inlayHint/resolve".to_string()),
            LawAxis::Custom("textDocument/codeAction".to_string()),
            LawAxis::Custom("textDocument/codeLens".to_string()),
            LawAxis::Custom("textDocument/definition".to_string()),
            LawAxis::Custom("textDocument/documentHighlight".to_string()),
            LawAxis::Custom("textDocument/documentLink".to_string()),
            LawAxis::Custom("textDocument/documentSymbol".to_string()),
            LawAxis::Custom("textDocument/foldingRange".to_string()),
            LawAxis::Custom("textDocument/formatting".to_string()),
            LawAxis::Custom("textDocument/inlayHint".to_string()),
            LawAxis::Custom("textDocument/inlineValue".to_string()),
            LawAxis::Custom("textDocument/linkedEditingRange".to_string()),
            LawAxis::Custom("textDocument/moniker".to_string()),
            LawAxis::Custom("textDocument/prepareCallHierarchy".to_string()),
            LawAxis::Custom("textDocument/prepareRename".to_string()),
            LawAxis::Custom("textDocument/prepareTypeHierarchy".to_string()),
            LawAxis::Custom("textDocument/rangeFormatting".to_string()),
            LawAxis::Custom("textDocument/references".to_string()),
            LawAxis::Custom("textDocument/rename".to_string()),
            LawAxis::Custom("textDocument/selectionRange".to_string()),
            LawAxis::Custom("textDocument/signatureHelp".to_string()),
            LawAxis::Custom("typeHierarchy/subtypes".to_string()),
            LawAxis::Custom("typeHierarchy/supertypes".to_string()),
            LawAxis::Custom("workspace/diagnostic".to_string()),
            LawAxis::Custom("workspace/symbol".to_string()),
        ],
        // Presence/behavior not yet determined
        unknown: vec![
            LawAxis::Custom("max/admission".to_string()),
            LawAxis::Custom("max/snapshot".to_string()),
            LawAxis::Custom("textDocument/semanticTokens/full".to_string()),
        ],
        ..Default::default()
    }
}